KMControllersSのdummy版を作るにあたって最低限変更したい機能

・モータオブジェクトの生成
	m = KMControllersS.USBController('/dev/ttyUSB0')
	getMotorDic()の中で使用する。
	ここでUSBControllerのインスタンスにidをもたせるか？
・read_SN().decode()
	motordicを作るときに必要
・enable()
・disable()
・reboot()
・close()
・free()
・interface(8)
・(saveAllRegisters())
・runForward()
・runReverse()
・stop()
・speed(num)
・maxTorque(num)
・presetPosition(num)
・moveTo(num)
・(moveTo_scaled(num))
・read_motor_measurement()
	return (position, velocity, torque)←これらはUSBControllerのメンバ変数(m_*)

その他
motordicはどうしよ。。。


-----
TODO & Ideas
・プログレスバーはメインウィンドウの下部に組み込みでいいかも(フローティングウィンドウだと迷子になる問題)
・Ops to execute:のあとにコマンド数が表示されるはず
・センサーウィンドウがメインにドッキングするといいかも
・センサーウィンドウの画像表示にズーム機能
・センサーウィンドウの画像表示がウィンドウの拡大縮小にリアルタイムに追従
・スクリプト実行中止後待たされないように
・現在のモーターの位置を読み取ってSpinBoxに反映するポタン
・ファイル/フォルダ選択の前回の位置を覚えておく機能
・クラス関係の整理(かんたんなクラス図を作って考える)
・新旧キャリブレータ両対応のソースにする
・キャリブレータのモータIDやscaleなどを設定ファイルから読むようにする
・PIDなどの設定機能
・ソースファイルの整形、いらないコメント/処理の整理
・センサーオープン時にシャッター速度/ゲインをUIの通りに設定
・複数スクリプトを連続実行

